

~~ Note-1: Created first controller using @controller and @GetMapping()

Directory source:
/src/main/java/com.urgekol.code_module1_Introduction/Controller/HomeController

code:
package com.urgekol.code_module1_Introduction.Controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class HomeController
{
    @Value("${spring.application.name}")
    private String appName;


    @RequestMapping("/")        // Rest API
    public String home()
    {
        System.out.println("HomeController mapping is active");

        return "index";         // Due to the thymeleaf, don't have to write "index.html"
    }
}

/*
    Must use the dependency:

    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-thymeleaf</artifactId>
    </dependency>
*/


Directory source:
/src/main/java/resources/templates/index.html

